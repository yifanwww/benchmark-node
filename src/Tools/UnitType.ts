export enum UnitType {
    /** Dimensionless number. */
    DIMENSIONLESS,
    /** Dimensionless integer. */
    DIMENSIONLESS_INTEGER,
    // XXX: maybe we don't need `ORIGIN`?
    ORIGIN,
    TIME,
}
